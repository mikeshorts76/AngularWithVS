(function () {

    var consentControllers = angular.module('consentConrtollers', []);

    consentController.controller('ConsentController', ['$scope', 
        function ($scope) {
        }]);

    consentController.controller('IdentificationController', ['$scope',
        function ($scope) {
        }]);

    consentController.controller('HistoryController', ['$scope',
        function ($scope) {
        }]);

    consentController.controller('VerifyController', ['$scope',
        function ($scope) {
        }]);

}());